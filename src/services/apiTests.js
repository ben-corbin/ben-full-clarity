// Tests to write for the API service

// Test fetchContacts() function:

// 1.1. Test a successful request:

// Mock the axios request to return a successful response.
// Call the fetchContacts() function.
// Check if the returned data matches the mocked response data.

// 1.2. Test a failed request:

// Mock the axios request to return an error response.
// Call the fetchContacts() function.
// Check if the console.error function is called with the error.

// Test createGroup() function:

// 2.1. Test a successful request:

// Mock the axios request to return a successful response.
// Call the createGroup() function with sample groupName, selectedContacts, and groupImage.
// Check if the returned data matches the mocked response data.

// 2.2. Test a failed request:

// Mock the axios request to return an error response.
// Call the createGroup() function with sample groupName, selectedContacts, and groupImage.
// Check if the console.error function is called with the error.
